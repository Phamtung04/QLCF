import React, { useEffect } from 'react';
import useAuth from 'hooks/useAuth';
import HDListRoom from './components/HDListRoom';

const PortalListRoom = () => {
    return <HDListRoom />;
};

export default PortalListRoom;
